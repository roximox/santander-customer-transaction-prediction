"""Dataset access utilities for OpenML dataset ID 45566."""


def load_dataset() -> None:
    """Load the Santander dataset in a future project phase.

    Raises:
        NotImplementedError: Always, because downloading is intentionally
            deferred until the data-audit phase.
    """
    raise NotImplementedError(
        "Dataset loading is not implemented yet; OpenML ID 45566 will be "
        "verified during the data-audit phase."
    )
